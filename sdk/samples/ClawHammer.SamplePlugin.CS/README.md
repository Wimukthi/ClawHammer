# Sample C# Plugin

This project builds a minimal ClawHammer plugin.

## Build
- `dotnet build`
- The output DLL is placed in `bin/Debug/net10.0` (or Release).

## Install
- Copy the DLL into the `plugins` folder next to `ClawHammer.exe`, or
- Use Plugin Manager -> Install From File.
